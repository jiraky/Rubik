package it.univr.rubikcube.model;

/**
 * Lateral column rotation direction.
 * @author Alessandro Menti
 */
public enum LateralColumnRotation {
    /** Rotation towards the left (as seen from the front). */
    LEFT,
    /** Rotation towards the right (as seen from the front). */
    RIGHT
}
